import 'package:flutter/material.dart';

class SleepViewMobile extends StatelessWidget {
  const SleepViewMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SleepPage"),
    );
  }
}
