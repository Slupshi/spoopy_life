import 'package:flutter/material.dart';

class SleepViewDesktop extends StatelessWidget {
  const SleepViewDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SleepPage"),
    );
  }
}
